<core:FragmentDefinition
	xmlns="sap.m"
	xmlns:core="sap.ui.core"
	xmlns:l="sap.ui.layout">
	<Dialog
        id="idPaymentDialog"
        title="Proceed to Payment"
        contentWidth="400px"
        afterClose=".onPaymentClose"
        class="sapUiContentPadding">
		<VBox width="100%" renderType="Bare">
			<!-- Payment Type Radio Buttons -->
			<RadioButtonGroup id="idPaymentTypeGroup" columns="2" select=".onPaymentTypeSelect">
				<buttons>
					<RadioButton text="UPI" selected="true" />
					<RadioButton text="Debit/Credit Card" />
				</buttons>
			</RadioButtonGroup>
			<HBox justifyContent="Start" class="sapUiSmallMarginTop">
				<Label text="Grand Total:" design="Bold" class="sapUiSmallMarginEnd"/>
				<Text text="{
                    path: 'HostelModel>/GrandTotal',
                    formatter: '.Formatter.formatCurrency'
                }" class="sapUiSmallMarginEnd"/>
			</HBox>
			<!-- UPI Section -->
			<VBox id="idUPISection" class="sapUiSmallMarginTop">
				<Label text="UPI ID" required="true"/>
				<Input id="idUPIID" placeholder="Enter your UPI ID" />
				<Image src="image/UPI.jpg" width="150px" class="sapUiMediumMarginTop" />
			</VBox>
			<!-- Card Section -->
			<VBox id="idCardSection" visible="false" class="sapUiSmallMarginTop">
				<Label text="Card Number" />
				<Input id="idCardNumber" placeholder="Enter Card Number" />
				<Label text="Expiry Date" />
				<Input id="idCardExpiry" placeholder="MM/YY" />
				<Label text="CVV" />
				<Input id="idCardCVV" type="Password" placeholder="***" />
			</VBox>
		</VBox>
		<!-- Common Fields (Visible for both types) -->
		<l:VerticalLayout width="100%" class="sapUiSmallMarginTop">
			<Label text="Bank Name" required="true"/>
			<Input id="idBankName" placeholder="Enter Bank Name" />
			<Label text="Amount" required="true"/>
			<HBox width="100%" justifyContent="Start" alignItems="Center">
				<VBox width="50%">
					<Input id="idAmount" type="Number" placeholder="Enter Amount" />
				</VBox>
				<VBox width="50%">
					<ComboBox id="idCurrency" items="{CurrencyModel>/}"  valueStateText="Select Currency"
              width="100%" change=".onFacilitybranchChange" placeholder="Select Currency">
						<core:Item  key="{CurrencyModel>currency}" text="{CurrencyModel>currency}"/>
					</ComboBox>
				</VBox>
			</HBox>
			<Label text="Payment Type" required="true"/>
			<Input id="idPaymentTypeField" placeholder="Enter Payment Type (e.g., Yearly)" />
			<Label text="Bank Transaction ID" required="true"/>
			<Input id="idTransactionID" placeholder="Enter Transaction ID" />
			<Label text="Date" required="true"/>
			<DatePicker id="idPaymentDate" displayFormat="yyyy-MM-dd" valueFormat="yyyy-MM-dd" />
		</l:VerticalLayout>
		<beginButton>
			<Button text="Submit Payment" type="Emphasized" press=".onSubmitPress" />
		</beginButton>
		<endButton>
			<Button text="Cancel" press=".onPaymentClose" />
		</endButton>
	</Dialog>
</core:FragmentDefinition>
