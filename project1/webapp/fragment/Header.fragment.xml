<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Bar
        design="Header"
        class="customRedBar"
    >
        <!-- Left side: Logo image -->
        <contentLeft>
            <Button visible="true" press="onNavBack" icon="sap-icon://nav-back" />
            <!-- <Image
                src=""
                alt="{i18n>landingPageHeaderTitle}"
                height="25px"
            /> -->
        </contentLeft>

        <!-- Right side: Logout button with icon -->
        <contentRight>
            <Button
                icon="sap-icon://log"
                press="onLogoutPress"
                class="customLogoutButton"
            />
        </contentRight>
    </Bar>
</core:FragmentDefinition>
